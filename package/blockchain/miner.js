module.exports = class Miner {
  constructor(miner_auth_password) {
    this.auth_password = miner_auth_password
  }

  GetBlockchain() { }

  ApiHandler = {
    StartHTTP: function () {

    }
  }
}
